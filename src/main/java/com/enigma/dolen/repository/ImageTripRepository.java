package com.enigma.dolen.repository;

import com.enigma.dolen.model.entity.ImageTrip;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ImageTripRepository extends JpaRepository<ImageTrip, String> {
}
